<%= form_for note do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :visible_to %>
  <%= f.text_field :visible_to %>
  <%= f.submit 'save' %>
<% end %>
<h1> Hi, here </h1>
<%= button_to("DELETE", note_path(note), method: :delete) %>
