<%= form_for note do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :visible_to %>
  <%= f.collection_select :id, User.all, :id, :email %>
  <!-- <%= f.text_field :visible_to %> -->
  <%= f.submit 'save' %>
<% end %>
